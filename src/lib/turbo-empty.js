/**
 * Stub resolusi untuk built-in Node di bundle client (Turbopack).
 * Setara dengan webpack resolve.fallback ...: false + Ignore node:
 */
const empty = Object.create(null);
export default empty;
